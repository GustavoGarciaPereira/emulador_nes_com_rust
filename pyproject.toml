[project]
name = "nes-emulator"
version = "0.1.0"        # ← essa linha está faltando
requires-python = ">=3.8"
dependencies = ["pygame"]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
module-name = "nes_core"